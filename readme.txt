Restful server that returns the best matched cases.

To update all dependencies:
  npm install

To get current version:
  npm version

Repo location:
  https://s-irv-webprod01.pathologypartners.intranet/repos/pbrain

To run a test:
  npm test

Run as:
  node index.js
